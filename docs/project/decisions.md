# Decisions

Append-only decision log. Each row: what was decided, why, how reversible it is, and
who decided. See [`fsamuels/sdlc-standards`](https://github.com/fsamuels/sdlc-standards)
for the format this table follows.

| ID | Date | Decision | Rationale | Reversibility | Decided by |
| --- | --- | --- | --- | --- | --- |
| D-1 | 2026-09-14 | Adopt `fsamuels/sdlc-standards` full layer (standards + skills) | Keep branch naming, docs organization, and PR process consistent with other projects on the same account from day one, rather than drifting and reconciling later | Easy — two `.claude/settings.json` keys plus a few vendored files | fsamuels |
| D-2 | 2026-09-14 | Choose Godot 4 as the engine | Fast iteration and low motivation-risk outweigh Unreal's higher photorealism ceiling, which this project isn't targeting anyway (see README non-goals); Godot's plain-text project files are also the only one of the three a Claude Code cloud session can meaningfully edit | Moderate — asset/logic work invested in Godot won't carry over automatically, though 3D assets (glTF/FBX, PBR textures) port to another engine if needed later | fsamuels |
| D-3 | 2026-09-14 | Start Phase 2 layout blockout before drone capture, using a provisional square boundary sized to the confirmed 24.06-acre parcel total and a placeholder building box, rather than waiting on real GIS/orthomosaic data | Makes visible progress possible while capture is pending; county GIS/parcel viewers are interactive-only and not fetchable headlessly, so exact boundary shape and building footprints aren't available yet | Easy — boundary mesh and building position are explicitly placeholder and get replaced wholesale once real data lands | fsamuels |
| D-4 | 2026-09-14 | Pull the real parcel boundary from the WA State Geospatial Open Data Portal's public `Current_Parcels` ArcGIS `FeatureServer`, queried directly by parcel ID, instead of exporting from the county's own GIS viewer | The county GIS viewer and property search site are JS-driven Esri apps with no visible download option; the state portal republishes the same county assessor parcel data through a public REST endpoint that returns GeoJSON directly, headlessly, superseding the "interactive-only" constraint noted in D-3 | Easy — a GeoJSON file with a documented query, no dependency introduced | fsamuels |
| D-5 | 2026-09-14 | Approximate building footprints from Overture Maps' ML-derived buildings dataset (public S3, queried via DuckDB) rather than tracing them by hand from satellite imagery, since no surveyed county building-footprint layer covers this parcel | Manual tracing from Google Maps/satellite imagery is slow and error-prone; Overture's dataset gives real polygon shapes and rough heights headlessly, at the cost of being an ML approximation rather than surveyed data | Easy — clearly labeled as approximate in `docs/parcel-data.md`; wholesale replacement once real/orthomosaic data lands, same as the D-3 placeholder building box | fsamuels |
| D-6 | 2026-09-14 | Replace the D-3 provisional square boundary and single placeholder building with the real boundary/building data (D-4, D-5) directly in `main.tscn`, using an equirectangular lon/lat→meters projection centered on the parcel's area centroid as the scene's local origin, and unit-sized `BoxMesh`/`BoxShape3D` sub-resources scaled per-node (rather than a distinct sized sub-resource per fence segment/building) | Keeps the .tscn small (13 fence segments + 7 buildings share 2 unit meshes total) instead of ~20 near-duplicate sized sub-resources; equirectangular projection is simple and sufficiently accurate at this scale (a ~450m-wide parcel) versus a full geodesic/UTM projection | Moderate — coordinate origin and axis convention (`+X`=east, `+Z`=south) are now a scene-wide assumption; any future real-world-data import (orthomosaic, drone terrain) needs to reuse the same convention or everything needs re-deriving | fsamuels |
| D-7 | 2026-09-14 | Texture the ground plane with public NAIP aerial imagery (USGS National Map `USGSNAIPPlus` ImageServer, queried headlessly for the same bbox as the ground plane's real-world extent) instead of the flat placeholder color, and store the fetched PNG under `project/textures/ground/` rather than `assets/orthomosaic/` | Free, public-domain, ~0.6–1m resolution imagery gives the case map a real-looking ground texture (roads, tree lines, structures roughly in place) while drone capture is pending, same "pull what's headlessly available now, replace wholesale later" pattern as D-4/D-5; `assets/` isn't reachable from Godot's `res://` root, so the texture the scene loads has to live inside `project/` | Easy — one PNG plus a texture reference in `main.tscn`; swapped out wholesale once the drone-derived orthomosaic (D-3's eventual replacement) is ready, same as the boundary/building placeholders | fsamuels |
| D-8 | 2026-09-14 | Extract the player, fence segment, and building out of `main.tscn` into their own reusable scenes (`player.tscn`, `fence_segment.tscn`, `building.tscn`), instanced into `main.tscn` with only a per-instance `transform` override, instead of each of the 20 fence/building copies carrying its own full node subtree inline | `main.tscn` was on track to grow linearly (~10-12 lines per object) through every future addition (Phase 4-6: vegetation, animals, more buildings), making the file increasingly slow to work with and diff-noisy; instancing means future additions are one node block each, and each shared scene can later diverge per-instance (e.g. a distinct texture per building) via child-node property overrides without touching the blueprint scene | Easy — behavior-preserving; every fence/building transform value was carried over unchanged, verified headlessly before/after | fsamuels |
| D-9 | 2026-09-14 | Use a free CC0 rigged/animated horse model (Quaternius's Farm Animal Pack, via [poly.pizza](https://poly.pizza/m/F8HAAcLeBL)) for the first horse, ahead of the plan's own ordering (Phase 5 was meant to wait until the base environment was solid; the user asked for a real model with wander movement now) | Modeling/rigging/animating a horse from scratch is far more effort than this project's placeholder-first pattern justifies; Quaternius's pack is public domain (no attribution required, unlike CC-BY), already rigged with Idle/Walk/Run/etc. animations, and ships as glTF — no format conversion needed for Godot | Easy — a single `.glb` file plus one wrapper scene (`horse.tscn`) and one script (`horse.gd`); swappable for a different or higher-fidelity model later without touching the wander logic | fsamuels |
| D-10 | 2026-09-14 | Use three "Poly by Google" models (Hen, Rooster, Coyote, via poly.pizza) at the user's direct request, even though — unlike the D-9 horse — they're CC BY 3.0 (attribution required) and unrigged (no animations; movement is a plain translate+rotate, no walk cycle) | These were the specific models the user linked; recorded here so the CC-BY attribution requirement doesn't get lost — satisfied by [docs/credits.md](../credits.md). The lack of animation is a real, disclosed limitation, not a bug: obj2gltf-converted Poly assets ship as a single static mesh with no armature | Easy to swap for a rigged/CC0 alternative later if the lack of a walk cycle bothers the user; the attribution requirement itself is a one-way constraint for as long as these specific files are in the repo | fsamuels |
| D-11 | 2026-09-15 | Replace the boxy placeholder fence with a procedurally-generated wire/post fence (`fence_segment.gd`, a `@tool` script driven by an exported `length` in meters), and add a separate treeline ring 20m outside the real parcel boundary (`tree_line_segment.gd`, same pattern) as the actual hard edge of the explorable map, using a free CC0 tree model (Quaternius, via [poly.pizza](https://poly.pizza/m/qZtx0AHhcy)) | The old fence (one `BoxMesh` non-uniformly scaled per instance) looked like a solid wall, not a real pasture fence, and doubled as both the property line and the world's hard edge — which reads as "boxed in" right at the boundary. Splitting the two (thin fence at the true line, natural-looking treeline further out as the actual limit) matches common open-world practice; per-segment procedural generation avoids the non-uniform-scale-stretches-post-thickness problem a static mesh would have across 13 segments ranging 4.8m–435m long. The treeline's outward direction per segment is computed from which side of each edge's midpoint is farther from the boundary polygon's centroid — a per-edge approximation, not a true polygon offset, so corners aren't mitered (acceptable for an irregular tree line, not for a precise fence) | Moderate — the fence scenes changed shape entirely (from a static mesh to a script-generated one), so `fence_segment.tscn` and every `Fence_XX`/`TreeLine_XX` transform in `main.tscn` changed together; reversible by reverting this commit, but not a small diff | fsamuels |
| D-12 | 2026-09-15 | Regenerate every `Fence_XX`/`TreeLine_XX` transform in `main.tscn` directly from the source GeoJSON, using the polygon's true area-weighted centroid as the projection origin, rather than continuing to reuse the D-6 boundary data (only partially fixed by the earlier straighten-fence-segments bugfix, which fixed orthogonality but not absolute direction) | Making the fence a real, visible wire/post fence (D-11) exposed that the boundary never actually traced the real polygon shape — several segments' directions were wrong (e.g. one stored as due-east was actually due-south) despite correct lengths and roughly correct positions, so the earlier bugfix's "make each box non-skewed" fix was necessary but not sufficient. Caught by the user visually reviewing D-11 in the editor before merging, not by any automated check. Verified the fix by cross-checking the new projection reproduces the existing building positions almost exactly and that walking the 13 edges closes the polygon with ~0 error | Easy — same pattern as D-6/the earlier bugfix, wholesale replacement of the affected transforms with no dependent code changes; the deeper lesson (verify against an independent source, like building positions or polygon closure, not just internal self-consistency like axis orthogonality) doesn't have a "revert" but should inform any future re-derivation of this data | fsamuels |
| D-13 | 2026-09-15 | Build the minimap (`scenes/minimap.tscn`, `scripts/minimap.gd`) as a static 2D overlay reusing the existing NAIP ground texture as its background, with the player's world position mapped onto it via a linear formula, rather than a live top-down `Camera3D`→`SubViewport` render | The NAIP texture already covers exactly the ground plane's 520m×520m extent 1:1 (D-7), so the world-to-map mapping is a single linear transform, not a rendering pipeline; a live camera would auto-reflect future world changes and could rotate with the player, but costs an extra render pass the static approach doesn't need. The mapping (`(world - ground_origin + half_extent) / extent` for both axes, no flip) was verified empirically — not assumed — by inspecting the actual `PlaneMesh` vertex/UV pairs, then cross-checked against a live scene: the player's spawn position lands at the expected pixel and the derived facing rotation matches the direction the player was hand-oriented to face (west, into the property, away from the road) | Easy — a self-contained scene/script pair with two exported constants (`ground_origin`, `ground_half_extent`); swappable for a live-rendered version later without touching how it's wired into `main.tscn` | fsamuels |
| D-14 | 2026-09-15 | Recompute each treeline segment's outward direction (D-11) by actually testing whether a point 5m past the segment in that direction falls outside the real polygon (ray-casting point-in-polygon), instead of the original "farther from the polygon's centroid" distance heuristic | The user spotted a treeline segment sitting inside the fence instead of outside it. The centroid-distance heuristic is only reliable for a roughly convex, roughly centroid-symmetric shape; this parcel isn't one (D-12 already established it's irregular with one 435m edge), and it got 2 of 13 segments backwards — both now confirmed correct by the same point-in-polygon test that also validated D-12's fix. This is the second time a "seems reasonable" geometric shortcut for this boundary turned out wrong under an actual correctness check rather than a plausibility check | Easy — only the 2 wrong `TreeLine_XX` transforms changed, fence positions were never affected by this bug | fsamuels |
| D-15 | 2026-09-15 | ~~Add 3 ducks... near coordinates interpreted as due west of the Back Barn... scale factors of 0.15, 0.4, and 54.2~~ — **superseded by D-17**: the west placement was wrong (user meant south) and 2 of the 3 scale factors were wrong (see D-17) | Same pattern as D-10: user-specified models, CC-BY so attribution-required (added to credits.md), unrigged/no animation (reuse `scripts/wander.gd`, no new script needed) | Easy — 3 small wrapper scenes reusing an existing script, no new code | fsamuels |
| D-16 | 2026-09-15 | Add 10 geese (Lesser snow goose, Poly by Google, CC BY 3.0), scattered in a 2-row cluster ~30m south of the duck cluster (D-15/D-17) | User asked for geese "near the ducks but far enough away you can tell they are separate" — 30m puts real distance between the two clusters (each bird's own wander radius is only 3-4m) while staying in the same general Back Barn area. All 10 share one wrapper scene/scale (unlike the ducks, which were 3 different models); no flocking behavior was requested, so each goose wanders independently like the ducks, not coordinated like D-10's chicken flock | Easy — one new wrapper scene reusing `scripts/wander.gd`, 10 plain instances in `main.tscn` | fsamuels |
| D-17 | 2026-09-15 | (1) Moved the D-15 ducks and D-16 geese from west of the Back Barn to south (the user's original ask, misread as west). (2) Fixed `duck_poly`'s and `duck_madtroll`'s scale factors — 0.0784 and 0.542, replacing 0.4 and 54.2. (3) Switched model-scale derivation from hand-parsing the raw glTF JSON's accessor `min`/`max` to actually instancing each `.glb` in the engine and computing the real, transform-aware `AABB` (`/tmp/inspect_model_aabb.gd`-style script). (4) Added 6 quail (Poly by Google, CC BY 3.0) near the player spawn | The user caught it directly: "the black duck is HUGE! it is taller than the horse!" `duck_madtroll`'s raw JSON bounding box was ~100x smaller than its real imported size (an embedded FBX unit-scale node transform the JSON-only parse never saw), so D-15's 54.2 scale factor — derived from that wrong raw number — produced a ~28m-tall duck. `duck_poly`'s raw JSON parse had grabbed the wrong accessor entirely (its real bounding box bore no consistent relationship to what was read before). Re-verified every existing animal (hen, rooster, coyote, goose, mallard duck) the same way: those five were already correct, so this was specifically a `duck_poly`/`duck_madtroll` bug, not a systemic one — **except the horse (D-9)**, which the same check puts at 6.9m tall (no scale is applied in `horse.tscn` at all). That one is flagged here but *not* fixed in this change — it was already reviewed and merged, so left for the user to decide on rather than changed unprompted | Easy — position/scale-only changes plus one new small wrapper scene; the horse-scale flag is a note, not a change, and reversible whenever the user wants it addressed | fsamuels |
| D-18 | 2026-09-15 | Fix the horse's scale: `horse.tscn`'s `Model` node now has an explicit `0.2317` scale (target ~1.6m tall), same engine-verified-AABB method as D-17 | The user asked to fix it right after D-17 flagged it. `horse.tscn` had applied no scale at all since D-9 — trusted "Quaternius packs are already at game-engine meter scale" without ever measuring it, unlike every other animal since (each of which got an explicit computed scale factor, not an assumed one). Re-verified after the fix: renders at 1.5999... ~1.6m | Easy — one line changed in `horse.tscn`, no script/behavior changes | fsamuels |
| D-19 | 2026-09-16 | Process the user's first drone capture (129 nadir, GPS-tagged photos of the Front Barn/Shop/Sally's House area, `assets/drone-source/front-section/`) into a real orthomosaic with OpenDroneMap (`opendronemap/odm` Docker image, `--fast-orthophoto --skip-report` — orthophoto only, no 3D mesh, since only a ground texture was needed), and add it to `main.tscn` as a new `FrontSectionOrthomosaic` node: a separate, higher-resolution ground patch (`PlaneMesh`, ~246m×173.5m, ~5cm/px) layered just above the existing NAIP ground plane, positioned/rotated from the orthophoto's own UTM 11N georeferencing rather than eyeballed | User-requested: "update part of the base map" from their own drone photos, the exact scenario `parcel-data.md`'s "still needed" list already flagged as pending. A separate patch (vs. pasting into the single 1024×1024 NAIP texture) was chosen specifically to keep the drone imagery's much higher detail instead of downsampling it away — the tradeoff (a resolution seam at its edges) is masked by using the orthophoto's own alpha channel (ODM marks pixels outside the actual flight coverage as transparent), so the patch's visible edge follows the true, irregular flight footprint rather than a hard rectangle. Placement was derived by converting the orthophoto's UTM corner coordinates through the same lon/lat→local-meters projection as the rest of the scene (D-6), with that projection's origin/offset calibrated against the 7 real building positions already in `main.tscn` (fit residual 0.7–2.2m, consistent with their known ML-approximation noise, D-5) rather than re-deriving the parcel-polygon centroid independently — this guarantees the new patch lines up with what's already placed, which matters more here than matching D-6's exact origin-derivation method. That fit revealed the patch is rotated ~1.147° relative to the scene's +X/+Z axes (UTM grid north isn't quite the scene's equirectangular south, at this location/scale) — small, but real over a 246m span, so it's baked into the node's transform rather than ignored. Verified two ways before committing to the transform: (1) algebraically, the transform reproduces all 4 real-world corner positions to within ~1cm; (2) an independently-coded top-down composite (raw geo data → pixel resample, no reuse of the Godot transform numbers) shows the road, buildings, and terrain lining up with the surrounding NAIP imagery with no visible offset | Moderate — the georeferencing/placement math is now validated and reusable for future drone flights (e.g. a Back Barn-area capture later), but each new patch still needs its own ODM run and corner re-projection; wholesale replacement of more of the NAIP base is still open, this is one section | fsamuels |
| D-20 | 2026-09-16 | ~~Texture the roofs of the 5 buildings covered by the D-19 orthomosaic... applied as a thin unit-`PlaneMesh` layered just above each building box~~ — **superseded by D-21**: a flat horizontal plane is invisible from ground level, which is exactly what it was meant to fix | User asked whether the new imagery could make the buildings "look more realistic" after seeing the D-19 patch; the roof-crop generation itself (resampling the orthophoto onto each building's footprint) was correct and reused unchanged by D-21, only the flat-plane geometry was wrong | Easy — superseded outright, no dangling references | fsamuels |
| D-21 | 2026-09-16 | Replace the D-20 flat roof caps with actual sloped (gable) roof geometry: two tilted `PlaneMesh` instances per building, ridge running along each building's longer footprint axis, pitch fixed at 20° applied to the shorter axis's half-span, textured with the roof crop split in half along the ridge and re-oriented per slope (`Buildings/<Name>_Roof_<North\|South\|East\|West>` in `main.tscn`) | The user reported "I really can't see the roofs when in the game" — a paper-thin horizontal plane presents almost zero visible cross-section from ground level or the side, exactly the failure mode the flat D-20 plane had. No real pitch/ridge-orientation data exists for these buildings, so pitch is a fixed assumption (20°, chosen as a plausible shallow ag-building angle) and ridge direction is a heuristic (runs along the longer footprint dimension) — confirmed against the roof photos themselves for 2 of 5 buildings (the ridge cap line is visible and lines up with the heuristic) but not verified for all 5. While building this, found and fixed a session-wide bug: Godot's flat `Transform3D(...)` constructor packs its 9 basis numbers in **row-major** order (`basis.x` = elements 0,3,6 — not 0,1,2 as assumed), so every rotated transform hand-authored earlier this session (D-19's ~1.147° patch rotation, and D-20's — even though D-20's own planes had no rotation and so weren't actually affected) had the rotation silently transposed/mirrored wherever it *was* applied. Axis-aligned (no-rotation, diagonal-only) transforms were unaffected, since a diagonal matrix's transpose is itself — which is why every earlier building/fence/tree-line transform looked correct despite this. Caught only by rendering actual in-engine screenshots (temporarily repositioning the player to stand near a roof) after a from-scratch Python cross-check of the D-19 math had passed — that check verified the *intended* geometry against itself but never actually exercised Godot's real parsing of the written `.tscn` text, so it couldn't have caught this | Moderate — the D-19 ground patch's rotation and all 10 roof-slope transforms needed correcting (computed via script, not by hand, to avoid re-introducing the same mistake); the row-major convention is now documented here for any future hand-authored rotated transform in this project | fsamuels |
| D-22 | 2026-09-16 | Steepen the fixed roof pitch from D-21's 20° to 30° for all 5 roofed buildings | User found the 20° roofs "still hard to see" while walking around even after D-21 fixed the flat-plane bug — a shallow pitch presents a smaller silhouette against the sky from typical ground-level viewing angles. Same "no real pitch data, pick a plausible fixed angle" caveat as D-21 applies; 30° is a more assertive but still ordinary ag-building angle | Easy — one constant changed, transforms regenerated by the same script as D-21 | fsamuels |
| D-23 | 2026-09-16 | Fill each roofed building's two open gable ends (the triangular gap between the box top and the D-21/D-22 roof peak) with a solid wall-colored triangle, via a new procedurally-generated `@tool` script/scene (`scripts/gable_end.gd`, `scenes/gable_end.tscn`) matching the existing fence/tree-line pattern (D-11) rather than a hand-authored mesh resource | User: "fill in the missing gables with the solid building wall... right now you can see right through them" — the sloped-roof geometry (D-21) only ever covered the two long sides, leaving the short ends open. A `SurfaceTool`-built triangle (exported `width`/`rise`, matching the building's short footprint span and the roof's rise) avoids hand-writing raw `ArrayMesh` vertex-buffer bytes in the `.tscn` text, which isn't practically hand-authorable. While verifying in-engine, one gable (Shop's west end) initially rendered as solid black instead of the wall tan — turned out to be `SSAO` strongly darkening a tight concave corner where the gable, roof edge, and box wall all meet at once (confirmed by toggling `ssao_enabled`/`shadow_enabled` off independently: shadows made no difference, disabling SSAO brought it back to a normal shaded tone), not a geometry/normal bug — the transform itself was verified correct both algebraically (proper-rotation determinant check) and by rendering the mirror-image East gable, which looked correct throughout | Easy — 10 new small instances plus one small reusable script/scene, no changes to the roof or box geometry they sit against | fsamuels |
| D-24 | 2026-09-15 | Fix the hen model's ground offset (`scenes/hen.tscn`'s `Model` node now translates `+0.232867` on Y) and move the whole flock 20 ft (6.096 m) north (`Flock`'s Z in `main.tscn`: -10 → -16.096) | User reported the chickens "are currently below the ground level and look like they are floating around." An engine-instanced AABB check (same method as D-17/D-18) showed `hen.glb`'s origin sits at the model's vertical center (Y bounds -38.81 to +38.27 raw units) rather than at its feet — unlike `rooster.glb`, whose origin was already at Y=0 — so the hens' legs/feet were buried and only the upper body showed above ground, reading as "floating." The rooster was unaffected and left unchanged. "North" reuses D-6's axis convention (+Z = south), so north is -Z | Easy — one Y-offset constant in `hen.tscn` and one Z coordinate in `main.tscn`, no script or model changes; re-verified headlessly (hen feet AABB lands at Y≈0 after the fix) | fsamuels |
| D-25 | 2026-09-15 | Fix `scripts/gable_end.gd`'s triangle by swapping its first two vertices (reversing winding) — **corrects, not just supersedes, D-23's diagnosis**: the gable ends were never an SSAO cosmetic issue, they were backface-culled (invisible) or lit with an inverted normal (mismatched brightness vs. the wall beside them), depending on which way a given building's gable happened to be rotated | User: "I am not seeing them" after D-23 shipped. Isolating a single gable node with everything else in the scene hidden (no box, no roof) proved it was rendering literally nothing from the outward-facing side — not dark, not present at all — which SSAO (a screen-space darkening effect on existing pixels) cannot cause. D-23's own "verified by rendering the mirror-image East gable" check only compared brightness across *different* compass-facing gables, which are expected to differ under directional sunlight (a north wall is legitimately dimmer than a south wall) — it never compared a gable against its own adjacent box wall, the actual same-orientation reference, which is what exposed the mismatch here. Re-verified per-gable this way (isolated node with cull/culling untouched, then side-by-side with its own wall) across all 5 buildings/10 gable ends after the fix: every one now blends seamlessly into the wall beside it, bright or shadowed alike | Easy — one two-line vertex-order swap in the shared script, no transform or per-instance changes; also a reminder that D-23's SSAO explanation should have been retired the moment a *different* explanation (visibility) explained the same symptom better, rather than left standing after only a single-instance visual check | fsamuels |
| D-26 | 2026-09-15 | Add a parked Triumph TR6 (`project/models/tr6.glb`, Configcars via Sketchfab, CC BY 4.0) east of the Shop, facing south — `scenes/tr6.tscn` (no wander script; it's a stationary prop, unlike every animal model so far), instanced once in `main.tscn` at `(215, 0, -52.205)` with no rotation (local forward is already `+Z`/south, unrotated). Before wiring it up, ran the as-downloaded `.glb` (32MB, 879k triangles, 2.6M render vertices — Sketchfab's "Autoconverted glTF" export, not optimized for real-time use) through `gltf-transform optimize` (`--compress false --texture-compress false --instance false --palette false --simplify true --simplify-ratio 0.2 --simplify-error 0.01`), cutting it to 7.7MB/~166k render vertices at this project's request ("very detailed... I plan for it to just sit there") | The model's size was almost entirely geometry, not textures (its 26 baked textures totaled under 3MB, already ≤512×512 for all but one) — a first default `optimize` pass (default `--instance`/`--palette` on) silently corrupted the mesh, exploding the bounding box from ~1.4m to ~219m on one axis (some part flew off under `EXT_mesh_gpu_instancing`), caught only by comparing `gltf-transform inspect`'s scene bbox before/after, not by eye — re-run with those two steps disabled reproduced the original bbox almost exactly (off by <0.001m) while still shrinking the file 4.2x, confirming the simplification itself was safe. Scale (`0.67`, applied in `tr6.tscn`'s `Model` node) and the ground-offset translate (`+0.945631` on Y, same proactive fix as D-24's hen rather than waiting for a "floating car" report) were both derived directly from `gltf-transform inspect`'s scene-graph-aware bounding box (no raw-JSON-accessor parsing, avoiding D-17's bug class) against the TR6's real-world dimensions (~4.2m L × 1.52m W × 1.22m H) — length and height matched a single 0.67 factor almost exactly, width came in slightly wide (mirrors). East/south placement follows D-6's `+X = east`/`+Z = south` convention, offset ~2m clear of the Shop's east gable wall (`x≈212.26`, D-23). Two things were guessed instead of verified before merge, and the user caught both by eye ("floating in the air... facing 180 degrees the wrong way"): (1) the root node's Y was copied from the Shop's own `y=1.96` on the assumption it reflected local terrain elevation — it doesn't; the ground plane is flat at `y=0` everywhere (confirmed by `Ground`'s own transform and every animal — Horse, Flock, Coyote — already sitting at `y=0`), and `1.96` is specific to the shared building-placeholder mesh's own vertical origin, not a value to reuse for an unrelated model. (2) the 180°-Y rotation assumed the model's local forward was `-Z` (the common Blender→glTF convention); it was already `+Z`, so the rotation faced it backwards — the intended headless Godot verification (same method as D-17/D-18/D-24) never ran, since the user's own editor held the project's import lock at the time | Moderate — the simplification is a one-way lossy edit to the shipped asset (the original 32MB download was discarded, not kept in the repo), reversible only by re-downloading from Sketchfab and re-running with different settings; the position/rotation fix was a one-line transform edit, no re-simplification needed | fsamuels |
| D-27 | 2026-09-16 | Add a live X/Z coordinate readout to the minimap HUD (`scenes/minimap.tscn`/`scripts/minimap.gd`, extending D-13), then use it to recenter the ducks (D-15/D-17) and geese (D-16) groups on user-supplied coordinates `(37.5, 17.9)`, keeping the two clusters 15 ft (4.572 m) apart instead of D-16's original 30 m — each cluster's internal formation preserved, only translated as a rigid group | User: "I don't have a good landmark other than just knowing myself" — repositioning animals by coordinate previously meant reading `Transform3D` off the `Player` node in the editor's Remote debugger tab (extra clicks, only available while running from the editor); a small always-visible label removes that friction going forward ("that will be very helpful as we develop"). The 15 ft gap is a deliberate reduction from D-16's 30 m — the user wants the two flocks closer together now, still just far enough to read as separate | Easy — the HUD addition is purely additive (new `Label`/`Panel` nodes in `minimap.tscn`, one new line in `minimap.gd`'s `_process`); the recenter is a translation-only transform change per bird, each cluster's shape unchanged | fsamuels |
| D-28 | 2026-09-16 | (1) Fix the horse model facing 180° backwards while walking — `horse.tscn`'s `Model` node now applies a 180°-Y rotation alongside its existing `0.2317` scale. (2) Switch `horse.gd` from radius-around-home wandering to roaming a shared exported rectangle (`roam_min_x/max_x/min_z/max_z`, defaulting to `(163.2, 30.6)`–`(249.1, 39.8)`, a pasture strip near Turn Out Shed 1 identified using the new minimap coordinate readout, D-27). (3) Add a second `Horse` instance (`Horse2` in `main.tscn`) roaming the same rectangle | User: "they look like they are moonwalking!" — `horse.gd`'s `look_at()` correctly orients the root node's `-Z` toward the direction of travel every frame, but the Quaternius model's own local forward was `+Z`, not `-Z` (the opposite mismatch from D-26's TR6, which was already correct) — so the horse's body faced *away* from where it was walking while the `Walk` animation's leg cycle kept playing normally relative to the body, producing exactly the moonwalk illusion (body facing backward, legs still "stepping forward" relative to itself). Never caught earlier because a single horse circling one fixed home point rarely gets watched walking toward the camera from enough angles to notice; adding a second horse and a long rectangular roam path made it obvious immediately. The rectangle replaces `WANDER_RADIUS`'s circle-around-spawn model because the user gave two opposite-corner coordinates directly, not a center-and-radius — `_pick_new_target()` now samples X/Z independently and uniformly within the rectangle instead of polar-sampling a circle | Easy — the rotation fix is a one-line transform change affecting both horse instances identically (no risk of only fixing one); the rectangle-roam refactor removed `home_position`/`WANDER_RADIUS` entirely in favor of exported bounds, a small script change with no effect on the other animals' separate `wander.gd` | fsamuels |
| D-29 | 2026-09-16 | Switch `tr6.glb`'s import from Extract Textures to Embed as Basis Universal (`gltf/embedded_image_handling`: 1 → 2), and track the resulting `tr6.glb.import` as a narrow `.gitignore` exception (`!project/models/tr6.glb.import`) instead of the usual gitignored/regenerated pattern | The user hit "missing dependencies" opening the project — Extract mode (this project's default for every other model, all single-texture) had pulled TR6's unusually large 25-material set out into loose `res://models/tr6_*.png` files that only ever existed in the local, gitignored `.godot` import cache; they were never committed, so `Failed loading resource: res://models/tr6_N.png` fired on any fresh reimport. Committing 25 loose PNGs was considered and rejected as messy for one model; embedding keeps `tr6.glb` a single self-contained asset like the repo-structure notes generally assume. Because `*.import` is gitignored project-wide, the embed setting itself needed an explicit tracked exception or a fresh clone would silently regenerate the default Extract config and reintroduce the same failure — verified by importing a fresh `git worktree` checkout of the branch with no prior `.godot` cache and confirming zero errors and no loose `tr6_*.png` files | Easy — a one-line import-setting flip plus one tracked file; reversible by restoring `gltf/embedded_image_handling=1` and re-committing extracted PNGs if per-texture editing is ever needed | fsamuels |
| D-30 | 2026-09-16 | Re-derive the position, footprint size, and Y-axis rotation of the 5 buildings covered by the D-19 front-section orthomosaic (Shop, Sally's House, Turn Out Shed 1, Turn Out Shed 2, Front Barn) directly from that orthomosaic's own pixel data, rather than continuing to trust the D-5 Overture Maps ML footprints — moving the buildings to match the drone imagery, not the other way around, since the orthomosaic is real georeferenced UTM data (D-19, verified to ~1cm) while the ML footprints are the acknowledged-approximate side (D-5, 0.7–2.2m residual). For each building: cropped the orthomosaic around its ML-approximated position, manually identified the 4 roof corners in pixel space against an overlaid coordinate grid, converted those pixel corners to scene-local meters through the same pixel→world transform baked into the `FrontSectionOrthomosaic` node (D-19/D-21's row-major `Transform3D` convention), then fit a center, width, depth, and rotation angle from the 4 corners. All 5 buildings' `Transform3D`s were regenerated (previously axis-aligned diagonal scale only), and their dependent roof/gable child transforms (D-21–D-23) were re-derived from the same corner data using the general rotation-aware version of those decisions' geometry formulas, since a rotated building needs rotated roof/gable pieces too | User: "I would like to better align the buildings to the drone photos. They're off enough that it bothers me, especially at the barn" — Front Barn turned out to need the largest correction: real footprint is ~16.3m × 20.1m (ML guess: 14.8m × 23.1m) centered about 4.7m from its old position and rotated ~4.5° off scene axes, which the old axis-aligned-only model couldn't represent at all. Turn Out Shed 1/2 also carry small real rotations (Turn Out Shed 1: ~-4.4°; Turn Out Shed 2: ~-0.3°, notably different from Turn Out Shed 1 despite both being small ag outbuildings — re-verified by eye against the imagery rather than assumed uniform). Shop and Sally's House turned out close to axis-aligned in the scene's own frame (small corrections only, ~2-4m position, modest size change) — plausibly because they're built compass/UTM-aligned, which after the D-19 ~1.147° UTM-vs-scene-grid rotation shows up as walking away from a scene-axis rotation of ~0°, while the barn/sheds are angled to a different real-world reference (a fence line, not compass north). Verified visually: overlaid each new rotated footprint back onto the orthomosaic (independent of the Godot scene) and confirmed the box now traces the real roof outline for all 5 buildings, closest for Shop/Sally's House/Front Barn, good-not-perfect for the two Turn Out Sheds given hand-picked pixel corners' inherent precision limit (sub-meter, not millimeter). House and Back Barn were left untouched — they sit outside or at the edge of the D-19 flight coverage and still need the second drone flight (D-19's noted future work) before their footprints can be corrected the same way | Moderate — 5 building transforms plus their 10 roof and 10 gable child transforms all changed together (each dependent on the others via the shared rotation-aware geometry formulas); reversible by reverting this commit, but not a small diff. The corner-picking itself is manual/visual, not survey-grade, so residual error is expected to be well under a meter rather than exactly zero — a further pass with a proper corner-marking tool would only refine, not fundamentally redo, this work | fsamuels |
| D-31 | 2026-09-16 | Three follow-up corrections to D-30, from the user's own on-the-ground knowledge rather than further image analysis: (1) Front Barn lengthened to a true 70 ft (21.336 m) end-to-end, extending only the south end (the north end was already correctly placed) — `sz` 20.03693→21.336, center and dependent roof/gable transforms shifted along the building's own rotated long axis by half the added length. (2) Added a new `Sally's House Garage` building instance (plain box, no roof texture yet) for the wing the user says sticks out on the north side toward the Shop — sized/positioned from the same orthomosaic-corner-picking method as D-30, using the distinct narrower roof section visible north of the main house's ridge line as its footprint (7.94m × 7.36m, same ~1.147° rotation as the main house, consistent with being part of the same structure). (3) Turn Out Shed 2 rotated further clockwise (as seen from above) — discovered while implementing that the sign convention linking increasing `theta` to on-screen rotation direction wasn't obvious from the math alone, so both directions (+3° and -3° from D-30's -0.257°) were rendered against the orthomosaic side by side before picking +3°, which visibly hugs the real roof edge much better than the -3° attempt did | User measured the barn "70 ft end to end" and confirmed it's long on the south side specifically, letting the fix target one end instead of scaling both. The Sally's House garage was invisible to the D-30 pass because the corner-picking there only looked at the main rectangular body's obvious 4 corners and never noticed the narrower roof extension further north was a separate, real footprint rather than background clutter — the user catching it from real-world knowledge is exactly the kind of error a photo-only pass can miss. The Turn Out Shed 2 rotation direction was verified empirically (render both signs, compare) rather than trusted from the coordinate-system reasoning alone, after that reasoning initially gave the wrong sign on the first attempt | Easy — all three are targeted edits (one building's length, one new sibling building instance, one building's rotation sign) independent of each other and of the rest of D-30's work; the garage has no roof/gable geometry yet (D-21-style textured roof for it is a natural follow-up, not done here) | fsamuels |
| D-32 | 2026-09-16 | Add two more real structures spotted in the D-19 orthomosaic at the user's direction: (1) `Turn Out Shed 3` near the large round pen by Front Barn (3.98m × 7.27m, rotated ~-8.3° — same corner-picking + fit method as D-30/D-31, with full sloped roof and gable geometry matching the other turnout sheds, but using a new flat-color `StandardMaterial3D_roof_tos3` instead of a cropped photo texture since no per-building roof crop exists for it). (2) `Red Shipping Container` west of Sally's House — a plain box (no roof geometry, since the user noted it's flat-roofed), sized to standard 40ft-container dimensions (12.19m × 2.44m × 2.59m) since the measured length from corner-picking (12.13m) matched a real 40ft container almost exactly while the measured width was unreliable (only ~1.85m from picking a very thin object's edges in a rotated crop), so the standard spec was trusted over the noisy pixel measurement for width/height | User: "add another turnout shed that is by the large round pin near the front barn... there's also a red shipping container to the west of Sally's house... does not need a roof because it has a flat roof." Both objects were located visually in wide context crops around Front Barn and Sally's House, then corner-picked the same way as every D-30/D-31 fix. The container's roofline shows a slight kink partway along its length (visible in the imagery) suggesting it may actually be two containers placed end-to-end at a slight angle rather than one — modeled as a single box for now since it reads fine as one prop from a distance; splitting it into two would be a small follow-up if it looks wrong up close | Easy — two new independent building instances, no changes to existing buildings; Turn Out Shed 3's roof color is a placeholder flat tint rather than a real photo crop, and the container is a single box rather than two if it's actually two units — both are candidates for a later touch-up pass, not blockers | fsamuels |
| D-33 | 2026-09-16 | Two corrections to D-32, both from the user's direct visual feedback: (1) `Turn Out Shed 3` shifted 1m east. (2) `Red Shipping Container` re-derived from only the container's larger, straighter roof section (the segment south of the visible kink) instead of averaging corners across both segments — the container's true rotation turned out to be ~1.147°, i.e. essentially axis-aligned with true UTM north/south like Shop and Sally's House, not the ~-18° D-32 computed by treating the far (kink-distorted) end as reliable. Length/width/height kept at D-32's standard-40ft-container values; only rotation and the small residual center shift changed | User: "Turn out shed 3 is slightly farther to the east" — verified by rendering a 1m eastward nudge against the orthomosaic before applying it, which visibly closed the gap to the real roof edge. On the container: "the image artifact... ignore and work around that. It is 1 box. The angle I see in green... is too far west" — confirmed by computing rotation from the two roof segments independently: the near/lower segment gave ~1.15° (matching the ortho's own UTM-vs-scene baseline, like other axis-aligned buildings) while the far/upper segment (across the kink) gave a very different ~-36° in isolation, proving the kink is exactly the kind of stitching displacement the user described, and that averaging across it (D-32's mistake) manufactures a fake ~-18° tilt that belongs to neither real segment | Easy — both are transform-only tweaks (one position, one rotation) to existing single-box instances, no new nodes or dependent geometry | fsamuels |
| D-34 | 2026-09-16 | Re-fit the `Red Shipping Container` a second time using automated color-segmentation (an HSV red mask + `cv2.minAreaRect`) instead of hand-picked corners, after D-33's manually-derived ~1.147° placement still put the box mostly over grass next to the roof rather than on it | User: "the red shipping container still looks wrong... Focus on the red corners. You included a lot of green" — direct evidence the manual corner-picking approach (D-30 through D-33) had run out of precision for an object this small and thin (2.4m wide, ~48px at this resolution): a few-pixel mis-click swings the fitted rotation by tens of degrees. Segmenting the actual red pixels and fitting a rotated rectangle removed the human-eyeballing error entirely. Fit the mask's upper and lower halves (split at the visible stitching kink) separately as a cross-check: their rotations agreed to ~1.3° of each other (~-3° and ~-4.5°) even though their absolute pixel positions differ — confirming, as the user said, that the kink is a local displacement artifact to route around, not a real angle difference between two segments, and that the manual picks' wild swings (1.1° to -18° across D-32/D-33 attempts) were pure human error, not signal. Final placement: center (194.29, -29.70), rotation -4.47° (the two segments' average) — coincidentally close to Front Barn's and the Turn Out Sheds' own ~-4.5° rotation, suggesting this part of the property shares a common real-world alignment distinct from the Shop/Sally's House/container-stitch-segment ~1.147° cluster. Length/width/height unchanged from D-32/D-33's standard 40ft-container values | Moderate lesson, easy fix — one transform line changed on an existing single-box instance; the real takeaway is procedural: for small or thin objects, prefer automated pixel-color fitting over manual corner-picking from the start, rather than iterating on hand-picks that can't converge | fsamuels |
| D-35 | 2026-09-16 | Replace the 3 turnout sheds' gabled roofs (two tilted `PlaneMesh` slopes + two triangular gable-end fills each, D-21/D-22/D-23/D-32) with single-slope lean-to roofs, low side to the west, and add a new `Pump House` building east of Sally's House (also a lean-to roof, low side south) with white walls instead of the shared beige box material — a new `@tool` script/scene (`scripts/lean_to_end.gd`, `scenes/lean_to_end.tscn`) builds the asymmetric right-triangle end fill (vertical edge on the high side, sloped hypotenuse to the zero-height low corner), reusing `gable_end.gd`'s vertex-winding fix (D-25) but with an added `high_side` param instead of a centered apex. Roof pitch dropped from the gabled roofs' 30° to 12° (within the user's 10-15° guess) and is now applied across the full building width instead of the half-width a ridge-and-two-slopes design used. All transforms computed via a Python script (not hand-authored) using the same row-major `Transform3D` convention documented in D-21, and cross-checked by re-deriving the existing gabled sheds' `Transform3D`s symbolically component-by-component before trusting the general formula for the new geometry — every re-derived end-piece origin matched the shipped gable data exactly, and the new roof-slope formula's `rise`/`width` terms matched independently for all 3 sheds | User: "clean up the turn out shed roofs. They are not gabled. They are lean-tos with maybe a 10-15 degree roof... with the low side on the west side of each building... also model the pump house which is to the east of Sally's house. It is also a lean-to with the low side to the south. Make the pump house white instead of beige" — while deriving the roof-panel placement formula, found the *existing* gabled roof panels (not gables) anchor their eave/ridge points from the building box's vertical center rather than its top (off by exactly `halfHeight`, i.e. the box's flat top visibly extends ~0.8m above the shipped roof ridge) — an apparent latent bug from D-21, distinct from the gable fill's own D-23/D-25 visibility bugs and never caught because the box's top only pokes slightly above the tilted plane rather than being fully exposed. Not fixed here (out of scope, the gabled sheds' geometry it lived in was replaced outright by this change) but worth a note for the remaining gabled buildings (Shop, Sally's House, Front Barn) if their roofs get revisited. No headless Godot binary is available in this session to render a screenshot for visual verification (unlike D-21/23/25's in-engine checks), so correctness here rests on the symbolic cross-check against the shipped D-21–D-32 data and a determinant/orientation check per new transform, not an actual render | Moderate — 3 buildings' roof/end geometry replaced (8 old nodes removed, 3 roofs + 6 ends + 1 new building + its roof + 2 ends added), plus 2 now-unused east-facing roof photo textures/materials dropped from the scene; reversible by reverting this commit. Not visually verified in-engine this session — a first in-game look is the natural follow-up before trusting the pitch/placement choices the way D-21/22's screenshots let those decisions be trusted | fsamuels |
| D-36 | 2026-09-16 | Regenerate the `turn_out_shed_1_west.png`/`turn_out_shed_2_west.png` roof-crop textures used by D-35's new lean-to roofs, by re-sampling the D-19 orthomosaic through each shed's *current* (post-D-30) roof-panel footprint instead of reusing the Sep-15 crops, which were generated before D-30 moved/resized/rotated these buildings on Sep-16 and so sampled the wrong patch of the orthomosaic ("part roof, part ground"). Re-derived the pixel↔world mapping analytically from the `FrontSectionOrthomosaic` node's own baked `Transform3D` (same row-major convention as D-21) plus each roof `MeshInstance3D`'s basis vectors, verified by inspecting Godot's actual `PlaneMesh` UV layout via a throwaway headless `SceneTree` script (`subdivide_width/depth` vertex dump) rather than assuming the U/V-to-corner convention, then resampled with a fitted affine transform (`PIL.Image.transform(AFFINE, ...)`) at the orthomosaic's native ~5cm/px density | User, while running PR #24 locally to verify it: "I'd also like to fix the texture of the roofs to match the updated location they were moved to a PR or 2 ago. right now they have part roof, part ground as the roof texture" — confirmed by file mtimes (all `textures/roofs/*.png` dated Sep 15, D-30's building move was Sep 16) and reproduced by rendering the old crops' implied footprint against the new building transforms, which fell mostly off the roof. The same staleness affects `shop_*`/`sallys_house_*`/`front_barn_*`, tracked separately in a bugfix branch since those buildings are untouched by this PR | Easy — two texture files replaced in place, same filenames/dimensions-class, no scene/transform changes; regenerable again from the same script if the orthomosaic or footprints change | fsamuels |
| D-37 | 2026-09-16 | Re-locate and re-size the `Pump House` (D-35) from its guessed placeholder position to the real structure visible in the D-19 orthomosaic: moved ~4.6m east / ~2.4m south (center (232, -27) → (236.645, -24.616)) and resized from a square 2.5m×2.5m footprint to a 4.24m(E-W)×2.0m(N-S) rectangle, keeping the same 12° pitch, "low side south" orientation, and white wall material. Found the real structure (a well pump house — visible pipes/wellhead equipment around it) by cropping the orthomosaic around the guessed position and looking for the "beige textured shingle roof in the middle of a dirt spot" the user described; pinned down its footprint with a labeled pixel grid (cross-checked against a color-segmentation core fit, which agreed on the north-south center to within 0.2px) rather than corner-fitting by eye alone, given how small the target is. Reused the pixel↔world and building/roof/end-piece transform relationships established in D-36/D-30 to convert the picked pixel rectangle into new node transforms; since the fix is a resize with no rotation change, the end pieces' rotation-only transforms (`lean_to_end.gd` builds its own mesh from `width`/`rise`, not a scaled shared mesh) only needed their origin and `width`/`rise` updated, not their basis vectors | User, after the D-35/D-36 fixes: "I also want to move the pump house to the correct spot. it needs to move a little farther east. there is a beige textured shingle roof in the middle of a dirt spot where it should be" — the original placement (this PR's own D-35) was a guess ("east of Sally's House") never corner-fit against the orthomosaic the way every other building on the property has been (D-30–D-34), unlike those, which this now brings the Pump House in line with | Easy — a further transform-only correction to a single building's 4 nodes, reversible, regenerable from the same method if the user spots more drift | fsamuels |
| D-38 | 2026-09-16 | Fix two roof bugs on the 3 buildings that still use the original gabled-roof design (Shop, Sally's House, Front Barn — everything else was either converted to lean-to or never had this bug, see D-35/D-37 above): (1) each gable roof's two sloped `MeshInstance3D` panels anchored their eave/ridge points from the building box's vertical *center* instead of its *top*, sinking the roof into the box by exactly the box's half-height — a latent bug introduced in D-21 and left unfixed through D-30's footprint updates; fixed by adding each building's half-height to its roof panels' Y-origin (Shop/Sally's House +1.96m/+2m, Front Barn +2m). (2) `shop_*`/`sallys_house_*`/`front_barn_*` roof-crop textures were generated Sep-15, before D-30 moved/resized/rotated these buildings on Sep-16, so the crops sampled the wrong patch of the D-19 orthomosaic ("part roof, part ground"); regenerated all 6 files the same way as D-36's turnout-shed fix, by re-deriving the pixel↔world mapping from the orthomosaic node's transform and each roof panel's current basis vectors, then resampling with a fitted affine transform at native ~5cm/px resolution | User, after pulling PR #24 (D-35/D-36 on that branch, unrelated to this fix — different repo state, same date) to test it locally: "something has really messed up the roofs. they are now too low for buildings with a gable. I'd also like to fix the texture of the roofs to match the updated location they were moved to a PR or 2 ago." Bug (1) was already flagged as a known-but-unfixed issue in the lean-to PR's own decision entry ("worth a note for the remaining gabled buildings... if their roofs get revisited") — confirmed here by checking the math on every roof node in the scene: Turn Out Sheds 1-3 and the new Pump House (all lean-to) anchor correctly from the box top, only the 3 remaining gabled buildings' panels are off by their half-height. Bug (2) confirmed by file mtimes (all pre-fix `textures/roofs/*.png` dated Sep 15, the D-30 building move was Sep 16) | Easy — 6 roof-panel Y-origin values changed (transform-only, reversible) and 6 texture files replaced in place at their existing filenames; both regenerable again from the same method if the orthomosaic or footprints change | fsamuels |
| D-39 | 2026-09-16 | Texture the `Red Shipping Container`'s top and sides with a real corrugated-metal photo crop instead of the shared beige placeholder color, using a new `StandardMaterial3D` (`textures/buildings/red_shipping_container.png`) with `uv1_triplanar` enabled so the same texture wraps all 6 box faces without manual UV unwrapping. Since no side-elevation photo exists for any building (only the nadir drone orthomosaic), derived the texture entirely from the container's own top-down crop: cropped a 39×49px (~1.95m×2.45m) tile from a *clean* section of the top, deliberately avoiding the ~1.3m stitching-seam artifact identified in D-33/D-34 (visible as a blurred double "kink" partway down the container's length in the orthomosaic); sized the tile to an exact whole number of corrugation-ridge periods (7px/ridge, measured from the row-brightness profile) so it tiles seamlessly top-to-bottom, and set `uv1_scale` from the tile's real-world physical size so the triplanar repeat matches the ridge spacing actually visible in the photo | User: "lets add some texture to the top and sides of the shipping container, use the top as a template, it can repeat easily, but also don't use the blured part in the middle cause from an error stiching" — confirmed the stitching kink by eye in a zoomed/gridded crop (two distinct discontinuities, not one), and picked the longest clean run (rows 0–128 of the 243-row top crop) to source the tile from | Easy — one new texture file plus a material/override addition on a single existing building instance; the same tile+triplanar approach is reusable for any other building once/if the wall-texturing backlog item (`docs/implementation-plan.md`) is picked up | fsamuels |
| D-40 | 2026-09-17 | Add a parked BMW 2002 tii (`project/models/bmw_2002_tii.glb`, Configcars/maxipub via Sketchfab, CC BY 4.0, user-downloaded) to Sally's House's driveway, facing north — `scenes/bmw_2002_tii.tscn`, instanced once in `main.tscn` at `(225.95, 0, -38.98)` with a 180°-Y rotation, no scale change. As with D-26's TR6, ran the as-downloaded `.glb` (22.63MB, 1.79M render vertices, Sketchfab's "Autoconverted glTF" export) through `gltf-transform optimize` (`--compress false --texture-compress false --instance false --palette false --simplify true --simplify-ratio 0.2 --simplify-error 0.01`), but this time the *default* `--join true` (mesh-joining/draw-call reduction, on by default in the `optimize` recipe and not one of the flags D-26 flagged) silently corrupted the model — bloating its Godot-imported bounding box from a correct 1.85m×1.41m×4.59m to 2.47m×3.90m×4.90m, asymmetrically per axis (not a uniform scale error). Root cause: this particular Sketchfab FBX→glTF export represents most mesh nodes as raw 4×4 `matrix` transforms (not decomposed TRS) with an unusual pattern — a `0.01`-scale-plus-rotation matrix on each mesh node, composed against parent translations already in meters — and `join`'s mesh-merging logic mishandles that decomposition, producing wrong vertex positions in the merged buffer. `gltf-transform inspect`'s own scene bbox on the *joined* output still reported the correct, uncorrupted bbox (stale/unrecomputed accessor bounds, not the actual post-join geometry) — meaning, unlike D-26, the inspect-diff check that caught that corruption does NOT catch this one; only instantiating the actual output in Godot and computing a real scene-graph AABB from `MeshInstance3D.get_aabb()` revealed the bloat. Found by bisecting `optimize`'s sub-flags (`--flatten`/`--join` on/off in isolation) against a from-scratch Godot headless AABB check, rather than trusting `inspect` a second time | The as-downloaded model was 22.63MB/1.79M vertices for a static parked-car prop; `--join` was left at its default (true) because D-26's TR6 fix only called out `--instance`/`--palette` as unsafe defaults, and `--join`'s own stated purpose (reducing draw calls) seemed unrelated to geometry correctness — this model's raw-matrix mesh-node structure turned out to be the actual trigger, not something D-26 encountered (TR6's meshes apparently use plain TRS nodes). Height stayed near-exact vs. the real BMW 2002 tii's ~1.41m (1.407m in the raw file) so no scale factor was applied, unlike TR6's 0.67 — width came in ~16% over the real 1.59m spec (mirrors, same reasoning as D-26) and length ~9% over the real 4.22m (bumper/overhang inclusion), both plausible without a re-scale. Driveway placement was derived by locating Sally's House in the `FrontSectionOrthomosaic` (D-19) via its known world-to-pixel transform, visually identifying the paved apron directly north of the "Sally's House Garage" wing (D-31) as the driveway, and converting a clear point on that pavement back to world coordinates through the same transform (inverse of D-19/D-30's pixel↔world math) — the "north" facing direction was determined by locating the model's own `calandre` (grille) and `aile_ar` (rear fender) sub-meshes and comparing their local Z positions (grille at local Z≈+2.29, rear fender at Z≈-0.99), confirming local +Z is the car's front (same convention as D-26's TR6), so a 180°-Y rotation was needed to point the nose at world -Z (north, per D-6) | Moderate — the simplification is a one-way lossy edit to the shipped asset (original 22.63MB download discarded, not kept in the repo); the `--join false` workaround is specific to this file's mesh-node encoding and would need re-discovering (or just always verified via the same Godot-AABB check rather than trusted from `inspect`) for any future Sketchfab import with a similar raw-matrix export. Also switched `bmw_2002_tii.glb`'s import to Embed as Basis Universal (`gltf/embedded_image_handling`: 1 → 2) proactively, same as D-29's TR6 fix, since this model's 11 baked textures would otherwise extract to loose untracked `res://models/bmw_2002_tii_*.png` files and break on a fresh clone — added the matching `!project/models/bmw_2002_tii.glb.import` `.gitignore` exception up front instead of waiting to hit the missing-dependencies error D-29 hit | fsamuels |
| D-41 | 2026-09-17 | Reposition the D-40 BMW 2002 tii twice, both times from the user's own coordinates rather than further orthomosaic analysis: first to put its grille at `(218.5, -38.7)`, then 2 ft further south to `(218.5, -38.09)` after seeing it in-game — final `BMW2002tii` transform in `main.tscn` is `(218.8421, 0, -36.34435)` (root position, offset from the grille point by the model's own fixed grille-to-root vector, unchanged by either move since no rotation changed) | D-40's own orthomosaic-derived driveway spot was off by several meters from where the user actually wanted the car, and by their own account "isn't quite correct" even after the first correction. Root cause, confirmed by dumping the `FrontSectionOrthomosaic` node's actual parsed `Transform3D` from a headless Godot script rather than hand-computing it from the `.tscn` text: D-40's placement math re-made the exact mistake D-21 already documents and warns about — Godot's flat `Transform3D(a,b,c,d,e,f,g,h,i,...)` text form is **row-major** (basis vectors are elements 0/3/6, 1/4/7, 2/5/8), not three consecutive (x,y,z) triples — so the ~1.147° orthomosaic rotation (D-19) got its off-diagonal terms sign-flipped in the pixel↔world derivation. At ~120m from the ground patch's own origin (it's 246m wide), that sign flip alone is enough to misplace a point by several meters, which matches the size of both corrections the user needed to make. The fix going forward isn't a one-time math correction — it's process: any future pixel↔world derivation from a `.tscn`-authored rotated transform must read the real `Transform3D` back out of the running engine (as D-36/D-38 already did for texture resampling) rather than hand-parsing the text a third time | Easy — two more transform-only edits to one existing node; the underlying lesson (verify rotated transforms via the engine, never by hand-parsing `.tscn` text) is now documented in three places (D-21, this entry, and implicitly D-36/D-38's own method) for any future orthomosaic-relative placement work | fsamuels |
| D-42 | 2026-09-17 | Re-fit the `Pump House` (D-35, D-37) footprint from 4.24m(E-W)×2.0m(N-S) down to 1.60m(E-W)×1.80m(N-S), and re-center it ~1.0m east / ~0.16m south (236.645,-24.616 → 236.717,-24.767) — all 4 dependent nodes (box, roof, both lean-to ends) re-derived via the same rotation-aware corner-fit formulas as D-30/D-37, at the same ~1.147° UTM-vs-scene rotation as the rest of this building cluster (Shop/Sally's House/Red Shipping Container) | User: "the pump house is too large and too rectangular. it is a much more square building footprint. it looks to be too far extended mostly west." Corner-picking the real structure directly from raw pixel color was unreliable here — plain RGB thresholding for the roof's reddish-brown tone also caught the surrounding disturbed dirt (same hue family, "not a lot of contrast" as the user warned), producing a mask that bled into the access path and grass edge. Switched to a Sobel/`FIND_EDGES` pass on the grayscale crop instead: the roof's shingle texture reads as a locally-smoother, lower-edge-density block against the dirt's higher-frequency salt-and-pepper noise, which cleanly separated the two even where color alone couldn't — the roof's white ridge-cap flashing also shows up as an unambiguous bright line in the edge image, independently confirming the top edge and width. First attempt (plain color read) still got the direction of the fix right (smaller, shifted east) but the edge-detected re-pick moved the corners further (net footprint 1.60×1.80 vs an intermediate 1.50×2.00 pass), so the edge-detection method is the one to reach for first next time a small, low-contrast structure needs corner-picking, not a fallback after color thresholding struggles | Easy — 4 existing nodes' transforms (plus 2 lean-to `width`/`rise` params) changed in place, same footprint-to-transform formulas as D-30/D-37 reused rather than re-derived from scratch; fully reversible and re-fittable again if the corners are still off | fsamuels |
| D-43 | 2026-09-17 | Shrink `Sally's House`'s footprint from 17.97m to 15.40m wide (E-W), keeping the west edge fixed and pulling the east edge in ~2.56m, plus regenerate both `sallys_house_north.png`/`sallys_house_south.png` roof-crop textures at the new (narrower) width — building box, both roof panels, and the east gable-end (`SallysHouse_Gable_East`) all re-derived from the corrected width using the same rotation-aware formulas as D-30/D-37/D-42 (pump house) | User: "there is a red mulch flowerbed on the east side of the house that is both included in the size of the building and the roof image. we need to resize that." Confirmed visually: a ~2m-wide mulched bed with black plastic landscape edging runs along the house's east wall in the D-19 orthomosaic, and the building's old east edge extended roughly 2.5m past the real roofline, into and past the bed. The roof-crop textures (D-36/D-38's resampling method) had been generated from that same oversized footprint, so the mulch bed's red-brown color was baked directly into both roof photo crops' east edge — visually confirmed by opening the existing PNGs before touching anything. Re-derived the pixel↔world sampling for the new textures the same way as D-36/D-38 (roof panel's own basis vectors, not a hand-assumed UV convention), but this time verified the U/V-to-corner mapping empirically two ways before trusting it: (1) dumped `PlaneMesh`'s real vertex/UV pairs from a running Godot scene (as D-36 did) to confirm size-independent PlaneMesh UV formula, (2) rendered the *old* footprint through the new script and confirmed it pixel-matched the already-shipped (known-correct) texture files before generating the new, smaller ones — a stronger check than D-36/D-38 ran, since a wrong flip here would have silently produced a mirrored roof no one would necessarily notice at a glance | Moderate — 4 geometry nodes' transforms changed via the same reversible formula-driven approach as D-30/D-37, plus 2 texture files regenerated in place at new (smaller) dimensions; both are re-derivable again from the same scripts if the corners need further adjustment | fsamuels |
| D-44 | 2026-09-17 | Add the first real (non-drone) exterior wall textures: 6 ground-level phone photos of Sally's House and its garage, taken by the user and checked in at `assets/reference-photos/sallys_house/` (compass-direction-named per wall, e.g. `house_north.jpg`, `garage_east.jpg`), cropped to 4 usable wall faces (house north/south, garage north/east — the other 4 faces of the two buildings have no photo yet) and applied as new `SallysHouse_Wall_*`/`SallysHouseGarage_Wall_*` `MeshInstance3D` decal planes layered flush (2cm outward offset to avoid z-fighting) against the existing placeholder boxes, same construction as the roof/gable planes (shared unit `PlaneMesh`, per-face `Transform3D` derived from the building's own basis vectors via the row-major convention from D-21) | User took the photos after asking where to put and how to name them; this fills the "texture each building's exterior walls" backlog item flagged as current priority in `implementation-plan.md` and previously blocked on the lack of any side-elevation photo (D-39). Crop boxes were picked by eye against a generated pixel-grid overlay (same manual technique as D-30's corner-picking) and are a simple axis-aligned crop-and-stretch to each wall's existing box width/height — not a true perspective-rectified (homography) correction — since these are ground-level phone shots with real keystoning, unlike the roof crops' near-orthographic drone source. While picking crops, found the photos' own proportions (e.g. the south wall reads ~5.4:1 width:height) are much wider/shorter than the existing placeholder box's 15.4m×4m (3.85:1) — the 4m wall height baked into every building box since the original ML placeholder pass looks like a significant overestimate for a real single-story ranch house/garage (more like ~2.7-3m), but correcting it is out of scope here (would ripple into every roof/gable transform that anchors off the box's half-height, per D-38) and is called out as a follow-up rather than silently fixed | Easy to re-crop or re-map (new texture files plus 4 new sibling nodes, nothing else changed) but the underlying box-height mismatch, if fixed later, is a Moderate ripple through the same roof/gable formulas D-38 already had to correct once; not verified with an in-engine screenshot — a headless Godot render was attempted but killed by the host running low on memory, so this rests on the same formula reuse that D-30/D-36/D-42/D-43 established rather than a rendered check, and should be eyeballed in the actual editor before trusting the wall placement/orientation | fsamuels |
| D-45 | 2026-09-17 | Fix two bugs in D-44's wall-plane `Transform3D`s: (1) every wall texture rendered upside down — the plane's local +Z axis (mapped to world vertical) had the wrong sign; confirmed the real convention by dumping `PlaneMesh.surface_get_arrays(0)`'s vertex/UV pairs from a throwaway headless `SceneTree` script (no GPU render needed, so it sidesteps D-44's memory-pressure limitation) rather than assuming it a second time. (2) 3 of the 4 walls (house-north, garage-north, garage-east) were also horizontally mirrored — the plane's local +X axis was mapped to the wrong compass direction; re-derived from a general "camera-right = (-Fz, 0, Fx) for a camera facing compass direction F" formula and cross-checked against known building positions (e.g. confirmed the garage sits *west* of the house's center via its `Transform3D` origin, not east as a first guess from the photo assumed) before trusting it | User: "I looked earlier and the images were upside down." No render was available to catch this before shipping D-44 (memory-pressure kill), so this iteration deliberately used a verifiable-without-rendering method instead of guessing again | Easy — 4 existing nodes' `Transform3D`s corrected in place, no geometry/texture files touched, same formula-driven/reversible pattern as every other transform fix in this log | fsamuels |
| D-46 | 2026-09-18 | Replace D-44's crop-and-stretch approach with a real perspective correction, and hand corner-picking to the user instead of guessing crop boxes from a static image: built `tools/elevation-rectifier.html`, a standalone page (also published as a Claude Artifact for the round-trip save) where the user drags 4 corner handles onto a photo's actual wall edges — with a live magnified loupe and arrow-key nudging for pixel precision — and the page computes a true projective (homography) rectification to the wall's real width/height, live-previewed while dragging. Re-cropped 3 of the 4 D-44 walls this way (house north/south, garage north) and replaced their texture files in place, same filenames, no scene changes needed since the rectifier outputs the same aspect ratios; `sallys_house_garage_east_wall.png` is still D-44's cruder crop-and-stretch, not yet redone | User: "little effort was made to crop out the parts of the photos [that] weren't the house" — a fair complaint; hand-picked axis-aligned crop boxes eyeballed from a static grid overlay can't correct for a ground-level photo's real keystoning the way the roof crops' near-orthographic drone source didn't need to. Letting the user pick the 4 corners directly, with a zoomed loupe for precision, both fixes the perspective distortion and puts the judgment call (where exactly is the wall's edge under that porch overhang) in the hands of the person who can see the real building, not a hand-eyeballed pixel grid | Easy — texture files swapped in place at their existing paths/dimensions; the tool itself is a reusable, project-committed asset (`tools/elevation-rectifier.html`) for every remaining wall on every remaining building, not a one-off script | fsamuels |
| D-47 | 2026-09-18 | Hold off wiring a texture onto `SallysHouse_Gable_East`: the user's new east-side photo (rectified via the D-46 tool, saved as `project/textures/buildings/sallys_house_east_wall_RAW_pending_geometry.png`, not yet referenced by `main.tscn`) shows the east side is a full covered porch/entry addition — its own windows, a door, posts, and railing — not a plain triangular gable end like the garage's. Mapping that rectangular photo onto the existing flat triangular gable placeholder would misrepresent the geometry, not just the texture, so it's left out of the scene until the porch gets real geometry (volume, roofline, window/door cutouts) | User: "I just added the east side of the house too. we will need to improve on that later. the way the house is designed, the gable will need to be modeled too" — confirms this is a known, deliberate gap, not an oversight; matches what the photo itself shows | Easy — nothing in the scene changed, one clearly-named pending texture file added that a future porch-geometry pass will consume; reversible by deleting it if the approach changes before then | fsamuels |
| D-48 | 2026-09-18 | Reverse D-47's hold-off and wire the east-side photo in anyway, as an explicit stopgap: renamed `sallys_house_east_wall_RAW_pending_geometry.png` → `sallys_house_east_wall.png` and added `SallysHouse_Wall_East`, a flat decal plane on the box's east face, same construction/formula as the other D-44/D-46 walls (`Transform3D` derived from the house's own basis vectors, face="east"). The known mismatch from D-47 (a rectangular porch photo on a box with a separate triangular gable mesh above it) is unchanged and still tracked in `implementation-plan.md`'s "model the east porch" backlog item — this just means something real is visible there now instead of the placeholder color, understood to look off around the roofline until that geometry work happens | User: "go ahead and wire it in for now and note we need to improve later" — explicit instruction to accept the interim visual mismatch rather than wait | Easy — one new sibling node/material/texture reference, same reversible pattern as every wall added so far; superseded outright once the porch gets real geometry | fsamuels |
| D-49 | 2026-09-18 | Reverse D-44's "commit reference photos normally" call: add `assets/reference-photos/` to `.gitignore` (same treatment as `assets/drone-source/`) and untrack the 7 photos already committed in D-44/D-47 (`git rm --cached`, files kept on disk, not deleted) | User: "I though we were going to put the reference photos in gitignore" — D-44 had actually decided the opposite (small volume, commit normally, "add a gitignore carve-out ... if it turns out to be dozens of large files"), but the user's own stated plan is to eventually photo-texture every building, which will in fact reach that volume; also unnecessary now that `tools/elevation-rectifier.html` (D-46) never uploads the raw photo anyway, only the rectified crop, so nothing in the actual workflow depends on these being tracked | Easy — a `.gitignore` addition plus an untrack; the 7 files already committed in D-44/D-47's history stay there (PR #30 not yet merged at the time of this decision, so history isn't rewritten to remove them — untracking only stops *future* photos from being added) | fsamuels |
| D-50 | 2026-09-18 | Extend `tools/elevation-rectifier.html` with a "trace profile" mode: after the usual 4-corner wall rectangle, the user can click additional points anywhere on the same photo and get each one's exact real-world (x, y) in meters, computed by inverting the same homography (added `invertUV`, the algebraic inverse of the existing `mapUV`/Heckbert square-to-quad mapping) — valid anywhere on that flat plane, not just inside the original rectangle, since a gable-end wall's siding is one continuous flat plane from ground to ridge. Renders a live to-scale "blueprint" (grid, wall outline, traced polyline, per-point coordinates) and, when the traced shape rises above the wall rectangle, an automatic roof-pitch readout (rise/run/angle from the highest traced point, assuming a centered ridge). Saves the profile's real coordinates plus a blueprint PNG alongside the usual crop | User wants to (1) show the real shape of Sally's House's east side, which "is actually more complicated than a simple rectangle with a triangle on top because of the eaves," and (2) get a *measured* roof pitch for the garage from `garage_north.jpg` instead of another guess, since "They were just made up numbers before" — one capability (real-world coordinates for any point on the photographed plane) serves both, rather than building two separate features | Moderate — meaningfully more code than prior tool changes (new inverse-homography math, a second canvas, a second save payload), but self-contained to the tool; verified the new math directly with a standalone round-trip test (uv→xy→uv across near-rectangular, strongly-perspective, and axis-aligned quads, including points above/outside the base rectangle) since the tool itself can't be exercised headlessly the way the Godot scene can | fsamuels |
