# Decisions

Append-only decision log. Each row: what was decided, why, how reversible it is, and
who decided. See [`fsamuels/sdlc-standards`](https://github.com/fsamuels/sdlc-standards)
for the format this table follows.

| ID | Date | Decision | Rationale | Reversibility | Decided by |
| --- | --- | --- | --- | --- | --- |
| D-1 | 2026-09-14 | Adopt `fsamuels/sdlc-standards` full layer (standards + skills) | Keep branch naming, docs organization, and PR process consistent with other projects on the same account from day one, rather than drifting and reconciling later | Easy — two `.claude/settings.json` keys plus a few vendored files | fsamuels |
| D-2 | 2026-09-14 | Choose Godot 4 as the engine | Fast iteration and low motivation-risk outweigh Unreal's higher photorealism ceiling, which this project isn't targeting anyway (see README non-goals); Godot's plain-text project files are also the only one of the three a Claude Code cloud session can meaningfully edit | Moderate — asset/logic work invested in Godot won't carry over automatically, though 3D assets (glTF/FBX, PBR textures) port to another engine if needed later | fsamuels |
| D-3 | 2026-09-14 | Start Phase 2 layout blockout before drone capture, using a provisional square boundary sized to the confirmed 24.06-acre parcel total and a placeholder building box, rather than waiting on real GIS/orthomosaic data | Makes visible progress possible while capture is pending; county GIS/parcel viewers are interactive-only and not fetchable headlessly, so exact boundary shape and building footprints aren't available yet | Easy — boundary mesh and building position are explicitly placeholder and get replaced wholesale once real data lands | fsamuels |
| D-4 | 2026-09-14 | Pull the real parcel boundary from the WA State Geospatial Open Data Portal's public `Current_Parcels` ArcGIS `FeatureServer`, queried directly by parcel ID, instead of exporting from the county's own GIS viewer | The county GIS viewer and property search site are JS-driven Esri apps with no visible download option; the state portal republishes the same county assessor parcel data through a public REST endpoint that returns GeoJSON directly, headlessly, superseding the "interactive-only" constraint noted in D-3 | Easy — a GeoJSON file with a documented query, no dependency introduced | fsamuels |
| D-5 | 2026-09-14 | Approximate building footprints from Overture Maps' ML-derived buildings dataset (public S3, queried via DuckDB) rather than tracing them by hand from satellite imagery, since no surveyed county building-footprint layer covers this parcel | Manual tracing from Google Maps/satellite imagery is slow and error-prone; Overture's dataset gives real polygon shapes and rough heights headlessly, at the cost of being an ML approximation rather than surveyed data | Easy — clearly labeled as approximate in `docs/parcel-data.md`; wholesale replacement once real/orthomosaic data lands, same as the D-3 placeholder building box | fsamuels |
| D-6 | 2026-09-14 | Replace the D-3 provisional square boundary and single placeholder building with the real boundary/building data (D-4, D-5) directly in `main.tscn`, using an equirectangular lon/lat→meters projection centered on the parcel's area centroid as the scene's local origin, and unit-sized `BoxMesh`/`BoxShape3D` sub-resources scaled per-node (rather than a distinct sized sub-resource per fence segment/building) | Keeps the .tscn small (13 fence segments + 7 buildings share 2 unit meshes total) instead of ~20 near-duplicate sized sub-resources; equirectangular projection is simple and sufficiently accurate at this scale (a ~450m-wide parcel) versus a full geodesic/UTM projection | Moderate — coordinate origin and axis convention (`+X`=east, `+Z`=south) are now a scene-wide assumption; any future real-world-data import (orthomosaic, drone terrain) needs to reuse the same convention or everything needs re-deriving | fsamuels |
| D-7 | 2026-09-14 | Texture the ground plane with public NAIP aerial imagery (USGS National Map `USGSNAIPPlus` ImageServer, queried headlessly for the same bbox as the ground plane's real-world extent) instead of the flat placeholder color, and store the fetched PNG under `project/textures/ground/` rather than `assets/orthomosaic/` | Free, public-domain, ~0.6–1m resolution imagery gives the case map a real-looking ground texture (roads, tree lines, structures roughly in place) while drone capture is pending, same "pull what's headlessly available now, replace wholesale later" pattern as D-4/D-5; `assets/` isn't reachable from Godot's `res://` root, so the texture the scene loads has to live inside `project/` | Easy — one PNG plus a texture reference in `main.tscn`; swapped out wholesale once the drone-derived orthomosaic (D-3's eventual replacement) is ready, same as the boundary/building placeholders | fsamuels |
| D-8 | 2026-09-14 | Extract the player, fence segment, and building out of `main.tscn` into their own reusable scenes (`player.tscn`, `fence_segment.tscn`, `building.tscn`), instanced into `main.tscn` with only a per-instance `transform` override, instead of each of the 20 fence/building copies carrying its own full node subtree inline | `main.tscn` was on track to grow linearly (~10-12 lines per object) through every future addition (Phase 4-6: vegetation, animals, more buildings), making the file increasingly slow to work with and diff-noisy; instancing means future additions are one node block each, and each shared scene can later diverge per-instance (e.g. a distinct texture per building) via child-node property overrides without touching the blueprint scene | Easy — behavior-preserving; every fence/building transform value was carried over unchanged, verified headlessly before/after | fsamuels |
| D-9 | 2026-09-14 | Use a free CC0 rigged/animated horse model (Quaternius's Farm Animal Pack, via [poly.pizza](https://poly.pizza/m/F8HAAcLeBL)) for the first horse, ahead of the plan's own ordering (Phase 5 was meant to wait until the base environment was solid; the user asked for a real model with wander movement now) | Modeling/rigging/animating a horse from scratch is far more effort than this project's placeholder-first pattern justifies; Quaternius's pack is public domain (no attribution required, unlike CC-BY), already rigged with Idle/Walk/Run/etc. animations, and ships as glTF — no format conversion needed for Godot | Easy — a single `.glb` file plus one wrapper scene (`horse.tscn`) and one script (`horse.gd`); swappable for a different or higher-fidelity model later without touching the wander logic | fsamuels |
| D-10 | 2026-09-14 | Use three "Poly by Google" models (Hen, Rooster, Coyote, via poly.pizza) at the user's direct request, even though — unlike the D-9 horse — they're CC BY 3.0 (attribution required) and unrigged (no animations; movement is a plain translate+rotate, no walk cycle) | These were the specific models the user linked; recorded here so the CC-BY attribution requirement doesn't get lost — satisfied by [docs/credits.md](../credits.md). The lack of animation is a real, disclosed limitation, not a bug: obj2gltf-converted Poly assets ship as a single static mesh with no armature | Easy to swap for a rigged/CC0 alternative later if the lack of a walk cycle bothers the user; the attribution requirement itself is a one-way constraint for as long as these specific files are in the repo | fsamuels |
| D-11 | 2026-09-15 | Replace the boxy placeholder fence with a procedurally-generated wire/post fence (`fence_segment.gd`, a `@tool` script driven by an exported `length` in meters), and add a separate treeline ring 20m outside the real parcel boundary (`tree_line_segment.gd`, same pattern) as the actual hard edge of the explorable map, using a free CC0 tree model (Quaternius, via [poly.pizza](https://poly.pizza/m/qZtx0AHhcy)) | The old fence (one `BoxMesh` non-uniformly scaled per instance) looked like a solid wall, not a real pasture fence, and doubled as both the property line and the world's hard edge — which reads as "boxed in" right at the boundary. Splitting the two (thin fence at the true line, natural-looking treeline further out as the actual limit) matches common open-world practice; per-segment procedural generation avoids the non-uniform-scale-stretches-post-thickness problem a static mesh would have across 13 segments ranging 4.8m–435m long. The treeline's outward direction per segment is computed from which side of each edge's midpoint is farther from the boundary polygon's centroid — a per-edge approximation, not a true polygon offset, so corners aren't mitered (acceptable for an irregular tree line, not for a precise fence) | Moderate — the fence scenes changed shape entirely (from a static mesh to a script-generated one), so `fence_segment.tscn` and every `Fence_XX`/`TreeLine_XX` transform in `main.tscn` changed together; reversible by reverting this commit, but not a small diff | fsamuels |
| D-12 | 2026-09-15 | Regenerate every `Fence_XX`/`TreeLine_XX` transform in `main.tscn` directly from the source GeoJSON, using the polygon's true area-weighted centroid as the projection origin, rather than continuing to reuse the D-6 boundary data (only partially fixed by the earlier straighten-fence-segments bugfix, which fixed orthogonality but not absolute direction) | Making the fence a real, visible wire/post fence (D-11) exposed that the boundary never actually traced the real polygon shape — several segments' directions were wrong (e.g. one stored as due-east was actually due-south) despite correct lengths and roughly correct positions, so the earlier bugfix's "make each box non-skewed" fix was necessary but not sufficient. Caught by the user visually reviewing D-11 in the editor before merging, not by any automated check. Verified the fix by cross-checking the new projection reproduces the existing building positions almost exactly and that walking the 13 edges closes the polygon with ~0 error | Easy — same pattern as D-6/the earlier bugfix, wholesale replacement of the affected transforms with no dependent code changes; the deeper lesson (verify against an independent source, like building positions or polygon closure, not just internal self-consistency like axis orthogonality) doesn't have a "revert" but should inform any future re-derivation of this data | fsamuels |
| D-13 | 2026-09-15 | Build the minimap (`scenes/minimap.tscn`, `scripts/minimap.gd`) as a static 2D overlay reusing the existing NAIP ground texture as its background, with the player's world position mapped onto it via a linear formula, rather than a live top-down `Camera3D`→`SubViewport` render | The NAIP texture already covers exactly the ground plane's 520m×520m extent 1:1 (D-7), so the world-to-map mapping is a single linear transform, not a rendering pipeline; a live camera would auto-reflect future world changes and could rotate with the player, but costs an extra render pass the static approach doesn't need. The mapping (`(world - ground_origin + half_extent) / extent` for both axes, no flip) was verified empirically — not assumed — by inspecting the actual `PlaneMesh` vertex/UV pairs, then cross-checked against a live scene: the player's spawn position lands at the expected pixel and the derived facing rotation matches the direction the player was hand-oriented to face (west, into the property, away from the road) | Easy — a self-contained scene/script pair with two exported constants (`ground_origin`, `ground_half_extent`); swappable for a live-rendered version later without touching how it's wired into `main.tscn` | fsamuels |
| D-14 | 2026-09-15 | Recompute each treeline segment's outward direction (D-11) by actually testing whether a point 5m past the segment in that direction falls outside the real polygon (ray-casting point-in-polygon), instead of the original "farther from the polygon's centroid" distance heuristic | The user spotted a treeline segment sitting inside the fence instead of outside it. The centroid-distance heuristic is only reliable for a roughly convex, roughly centroid-symmetric shape; this parcel isn't one (D-12 already established it's irregular with one 435m edge), and it got 2 of 13 segments backwards — both now confirmed correct by the same point-in-polygon test that also validated D-12's fix. This is the second time a "seems reasonable" geometric shortcut for this boundary turned out wrong under an actual correctness check rather than a plausibility check | Easy — only the 2 wrong `TreeLine_XX` transforms changed, fence positions were never affected by this bug | fsamuels |
| D-15 | 2026-09-15 | ~~Add 3 ducks... near coordinates interpreted as due west of the Back Barn... scale factors of 0.15, 0.4, and 54.2~~ — **superseded by D-17**: the west placement was wrong (user meant south) and 2 of the 3 scale factors were wrong (see D-17) | Same pattern as D-10: user-specified models, CC-BY so attribution-required (added to credits.md), unrigged/no animation (reuse `scripts/wander.gd`, no new script needed) | Easy — 3 small wrapper scenes reusing an existing script, no new code | fsamuels |
| D-16 | 2026-09-15 | Add 10 geese (Lesser snow goose, Poly by Google, CC BY 3.0), scattered in a 2-row cluster ~30m south of the duck cluster (D-15/D-17) | User asked for geese "near the ducks but far enough away you can tell they are separate" — 30m puts real distance between the two clusters (each bird's own wander radius is only 3-4m) while staying in the same general Back Barn area. All 10 share one wrapper scene/scale (unlike the ducks, which were 3 different models); no flocking behavior was requested, so each goose wanders independently like the ducks, not coordinated like D-10's chicken flock | Easy — one new wrapper scene reusing `scripts/wander.gd`, 10 plain instances in `main.tscn` | fsamuels |
| D-17 | 2026-09-15 | (1) Moved the D-15 ducks and D-16 geese from west of the Back Barn to south (the user's original ask, misread as west). (2) Fixed `duck_poly`'s and `duck_madtroll`'s scale factors — 0.0784 and 0.542, replacing 0.4 and 54.2. (3) Switched model-scale derivation from hand-parsing the raw glTF JSON's accessor `min`/`max` to actually instancing each `.glb` in the engine and computing the real, transform-aware `AABB` (`/tmp/inspect_model_aabb.gd`-style script). (4) Added 6 quail (Poly by Google, CC BY 3.0) near the player spawn | The user caught it directly: "the black duck is HUGE! it is taller than the horse!" `duck_madtroll`'s raw JSON bounding box was ~100x smaller than its real imported size (an embedded FBX unit-scale node transform the JSON-only parse never saw), so D-15's 54.2 scale factor — derived from that wrong raw number — produced a ~28m-tall duck. `duck_poly`'s raw JSON parse had grabbed the wrong accessor entirely (its real bounding box bore no consistent relationship to what was read before). Re-verified every existing animal (hen, rooster, coyote, goose, mallard duck) the same way: those five were already correct, so this was specifically a `duck_poly`/`duck_madtroll` bug, not a systemic one — **except the horse (D-9)**, which the same check puts at 6.9m tall (no scale is applied in `horse.tscn` at all). That one is flagged here but *not* fixed in this change — it was already reviewed and merged, so left for the user to decide on rather than changed unprompted | Easy — position/scale-only changes plus one new small wrapper scene; the horse-scale flag is a note, not a change, and reversible whenever the user wants it addressed | fsamuels |
| D-18 | 2026-09-15 | Fix the horse's scale: `horse.tscn`'s `Model` node now has an explicit `0.2317` scale (target ~1.6m tall), same engine-verified-AABB method as D-17 | The user asked to fix it right after D-17 flagged it. `horse.tscn` had applied no scale at all since D-9 — trusted "Quaternius packs are already at game-engine meter scale" without ever measuring it, unlike every other animal since (each of which got an explicit computed scale factor, not an assumed one). Re-verified after the fix: renders at 1.5999... ~1.6m | Easy — one line changed in `horse.tscn`, no script/behavior changes | fsamuels |
| D-19 | 2026-09-16 | Process the user's first drone capture (129 nadir, GPS-tagged photos of the Front Barn/Shop/Sally's House area, `assets/drone-source/front-section/`) into a real orthomosaic with OpenDroneMap (`opendronemap/odm` Docker image, `--fast-orthophoto --skip-report` — orthophoto only, no 3D mesh, since only a ground texture was needed), and add it to `main.tscn` as a new `FrontSectionOrthomosaic` node: a separate, higher-resolution ground patch (`PlaneMesh`, ~246m×173.5m, ~5cm/px) layered just above the existing NAIP ground plane, positioned/rotated from the orthophoto's own UTM 11N georeferencing rather than eyeballed | User-requested: "update part of the base map" from their own drone photos, the exact scenario `parcel-data.md`'s "still needed" list already flagged as pending. A separate patch (vs. pasting into the single 1024×1024 NAIP texture) was chosen specifically to keep the drone imagery's much higher detail instead of downsampling it away — the tradeoff (a resolution seam at its edges) is masked by using the orthophoto's own alpha channel (ODM marks pixels outside the actual flight coverage as transparent), so the patch's visible edge follows the true, irregular flight footprint rather than a hard rectangle. Placement was derived by converting the orthophoto's UTM corner coordinates through the same lon/lat→local-meters projection as the rest of the scene (D-6), with that projection's origin/offset calibrated against the 7 real building positions already in `main.tscn` (fit residual 0.7–2.2m, consistent with their known ML-approximation noise, D-5) rather than re-deriving the parcel-polygon centroid independently — this guarantees the new patch lines up with what's already placed, which matters more here than matching D-6's exact origin-derivation method. That fit revealed the patch is rotated ~1.147° relative to the scene's +X/+Z axes (UTM grid north isn't quite the scene's equirectangular south, at this location/scale) — small, but real over a 246m span, so it's baked into the node's transform rather than ignored. Verified two ways before committing to the transform: (1) algebraically, the transform reproduces all 4 real-world corner positions to within ~1cm; (2) an independently-coded top-down composite (raw geo data → pixel resample, no reuse of the Godot transform numbers) shows the road, buildings, and terrain lining up with the surrounding NAIP imagery with no visible offset | Moderate — the georeferencing/placement math is now validated and reusable for future drone flights (e.g. a Back Barn-area capture later), but each new patch still needs its own ODM run and corner re-projection; wholesale replacement of more of the NAIP base is still open, this is one section | fsamuels |
| D-20 | 2026-09-16 | ~~Texture the roofs of the 5 buildings covered by the D-19 orthomosaic... applied as a thin unit-`PlaneMesh` layered just above each building box~~ — **superseded by D-21**: a flat horizontal plane is invisible from ground level, which is exactly what it was meant to fix | User asked whether the new imagery could make the buildings "look more realistic" after seeing the D-19 patch; the roof-crop generation itself (resampling the orthophoto onto each building's footprint) was correct and reused unchanged by D-21, only the flat-plane geometry was wrong | Easy — superseded outright, no dangling references | fsamuels |
| D-21 | 2026-09-16 | Replace the D-20 flat roof caps with actual sloped (gable) roof geometry: two tilted `PlaneMesh` instances per building, ridge running along each building's longer footprint axis, pitch fixed at 20° applied to the shorter axis's half-span, textured with the roof crop split in half along the ridge and re-oriented per slope (`Buildings/<Name>_Roof_<North\|South\|East\|West>` in `main.tscn`) | The user reported "I really can't see the roofs when in the game" — a paper-thin horizontal plane presents almost zero visible cross-section from ground level or the side, exactly the failure mode the flat D-20 plane had. No real pitch/ridge-orientation data exists for these buildings, so pitch is a fixed assumption (20°, chosen as a plausible shallow ag-building angle) and ridge direction is a heuristic (runs along the longer footprint dimension) — confirmed against the roof photos themselves for 2 of 5 buildings (the ridge cap line is visible and lines up with the heuristic) but not verified for all 5. While building this, found and fixed a session-wide bug: Godot's flat `Transform3D(...)` constructor packs its 9 basis numbers in **row-major** order (`basis.x` = elements 0,3,6 — not 0,1,2 as assumed), so every rotated transform hand-authored earlier this session (D-19's ~1.147° patch rotation, and D-20's — even though D-20's own planes had no rotation and so weren't actually affected) had the rotation silently transposed/mirrored wherever it *was* applied. Axis-aligned (no-rotation, diagonal-only) transforms were unaffected, since a diagonal matrix's transpose is itself — which is why every earlier building/fence/tree-line transform looked correct despite this. Caught only by rendering actual in-engine screenshots (temporarily repositioning the player to stand near a roof) after a from-scratch Python cross-check of the D-19 math had passed — that check verified the *intended* geometry against itself but never actually exercised Godot's real parsing of the written `.tscn` text, so it couldn't have caught this | Moderate — the D-19 ground patch's rotation and all 10 roof-slope transforms needed correcting (computed via script, not by hand, to avoid re-introducing the same mistake); the row-major convention is now documented here for any future hand-authored rotated transform in this project | fsamuels |
| D-22 | 2026-09-16 | Steepen the fixed roof pitch from D-21's 20° to 30° for all 5 roofed buildings | User found the 20° roofs "still hard to see" while walking around even after D-21 fixed the flat-plane bug — a shallow pitch presents a smaller silhouette against the sky from typical ground-level viewing angles. Same "no real pitch data, pick a plausible fixed angle" caveat as D-21 applies; 30° is a more assertive but still ordinary ag-building angle | Easy — one constant changed, transforms regenerated by the same script as D-21 | fsamuels |
| D-23 | 2026-09-16 | Fill each roofed building's two open gable ends (the triangular gap between the box top and the D-21/D-22 roof peak) with a solid wall-colored triangle, via a new procedurally-generated `@tool` script/scene (`scripts/gable_end.gd`, `scenes/gable_end.tscn`) matching the existing fence/tree-line pattern (D-11) rather than a hand-authored mesh resource | User: "fill in the missing gables with the solid building wall... right now you can see right through them" — the sloped-roof geometry (D-21) only ever covered the two long sides, leaving the short ends open. A `SurfaceTool`-built triangle (exported `width`/`rise`, matching the building's short footprint span and the roof's rise) avoids hand-writing raw `ArrayMesh` vertex-buffer bytes in the `.tscn` text, which isn't practically hand-authorable. While verifying in-engine, one gable (Shop's west end) initially rendered as solid black instead of the wall tan — turned out to be `SSAO` strongly darkening a tight concave corner where the gable, roof edge, and box wall all meet at once (confirmed by toggling `ssao_enabled`/`shadow_enabled` off independently: shadows made no difference, disabling SSAO brought it back to a normal shaded tone), not a geometry/normal bug — the transform itself was verified correct both algebraically (proper-rotation determinant check) and by rendering the mirror-image East gable, which looked correct throughout | Easy — 10 new small instances plus one small reusable script/scene, no changes to the roof or box geometry they sit against | fsamuels |
| D-24 | 2026-09-15 | Fix the hen model's ground offset (`scenes/hen.tscn`'s `Model` node now translates `+0.232867` on Y) and move the whole flock 20 ft (6.096 m) north (`Flock`'s Z in `main.tscn`: -10 → -16.096) | User reported the chickens "are currently below the ground level and look like they are floating around." An engine-instanced AABB check (same method as D-17/D-18) showed `hen.glb`'s origin sits at the model's vertical center (Y bounds -38.81 to +38.27 raw units) rather than at its feet — unlike `rooster.glb`, whose origin was already at Y=0 — so the hens' legs/feet were buried and only the upper body showed above ground, reading as "floating." The rooster was unaffected and left unchanged. "North" reuses D-6's axis convention (+Z = south), so north is -Z | Easy — one Y-offset constant in `hen.tscn` and one Z coordinate in `main.tscn`, no script or model changes; re-verified headlessly (hen feet AABB lands at Y≈0 after the fix) | fsamuels |
| D-25 | 2026-09-15 | Fix `scripts/gable_end.gd`'s triangle by swapping its first two vertices (reversing winding) — **corrects, not just supersedes, D-23's diagnosis**: the gable ends were never an SSAO cosmetic issue, they were backface-culled (invisible) or lit with an inverted normal (mismatched brightness vs. the wall beside them), depending on which way a given building's gable happened to be rotated | User: "I am not seeing them" after D-23 shipped. Isolating a single gable node with everything else in the scene hidden (no box, no roof) proved it was rendering literally nothing from the outward-facing side — not dark, not present at all — which SSAO (a screen-space darkening effect on existing pixels) cannot cause. D-23's own "verified by rendering the mirror-image East gable" check only compared brightness across *different* compass-facing gables, which are expected to differ under directional sunlight (a north wall is legitimately dimmer than a south wall) — it never compared a gable against its own adjacent box wall, the actual same-orientation reference, which is what exposed the mismatch here. Re-verified per-gable this way (isolated node with cull/culling untouched, then side-by-side with its own wall) across all 5 buildings/10 gable ends after the fix: every one now blends seamlessly into the wall beside it, bright or shadowed alike | Easy — one two-line vertex-order swap in the shared script, no transform or per-instance changes; also a reminder that D-23's SSAO explanation should have been retired the moment a *different* explanation (visibility) explained the same symptom better, rather than left standing after only a single-instance visual check | fsamuels |
| D-26 | 2026-09-15 | Add a parked Triumph TR6 (`project/models/tr6.glb`, Configcars via Sketchfab, CC BY 4.0) east of the Shop, facing south — `scenes/tr6.tscn` (no wander script; it's a stationary prop, unlike every animal model so far), instanced once in `main.tscn` at `(215, 0, -52.205)` with no rotation (local forward is already `+Z`/south, unrotated). Before wiring it up, ran the as-downloaded `.glb` (32MB, 879k triangles, 2.6M render vertices — Sketchfab's "Autoconverted glTF" export, not optimized for real-time use) through `gltf-transform optimize` (`--compress false --texture-compress false --instance false --palette false --simplify true --simplify-ratio 0.2 --simplify-error 0.01`), cutting it to 7.7MB/~166k render vertices at this project's request ("very detailed... I plan for it to just sit there") | The model's size was almost entirely geometry, not textures (its 26 baked textures totaled under 3MB, already ≤512×512 for all but one) — a first default `optimize` pass (default `--instance`/`--palette` on) silently corrupted the mesh, exploding the bounding box from ~1.4m to ~219m on one axis (some part flew off under `EXT_mesh_gpu_instancing`), caught only by comparing `gltf-transform inspect`'s scene bbox before/after, not by eye — re-run with those two steps disabled reproduced the original bbox almost exactly (off by <0.001m) while still shrinking the file 4.2x, confirming the simplification itself was safe. Scale (`0.67`, applied in `tr6.tscn`'s `Model` node) and the ground-offset translate (`+0.945631` on Y, same proactive fix as D-24's hen rather than waiting for a "floating car" report) were both derived directly from `gltf-transform inspect`'s scene-graph-aware bounding box (no raw-JSON-accessor parsing, avoiding D-17's bug class) against the TR6's real-world dimensions (~4.2m L × 1.52m W × 1.22m H) — length and height matched a single 0.67 factor almost exactly, width came in slightly wide (mirrors). East/south placement follows D-6's `+X = east`/`+Z = south` convention, offset ~2m clear of the Shop's east gable wall (`x≈212.26`, D-23). Two things were guessed instead of verified before merge, and the user caught both by eye ("floating in the air... facing 180 degrees the wrong way"): (1) the root node's Y was copied from the Shop's own `y=1.96` on the assumption it reflected local terrain elevation — it doesn't; the ground plane is flat at `y=0` everywhere (confirmed by `Ground`'s own transform and every animal — Horse, Flock, Coyote — already sitting at `y=0`), and `1.96` is specific to the shared building-placeholder mesh's own vertical origin, not a value to reuse for an unrelated model. (2) the 180°-Y rotation assumed the model's local forward was `-Z` (the common Blender→glTF convention); it was already `+Z`, so the rotation faced it backwards — the intended headless Godot verification (same method as D-17/D-18/D-24) never ran, since the user's own editor held the project's import lock at the time | Moderate — the simplification is a one-way lossy edit to the shipped asset (the original 32MB download was discarded, not kept in the repo), reversible only by re-downloading from Sketchfab and re-running with different settings; the position/rotation fix was a one-line transform edit, no re-simplification needed | fsamuels |
| D-27 | 2026-09-16 | Add a live X/Z coordinate readout to the minimap HUD (`scenes/minimap.tscn`/`scripts/minimap.gd`, extending D-13), then use it to recenter the ducks (D-15/D-17) and geese (D-16) groups on user-supplied coordinates `(37.5, 17.9)`, keeping the two clusters 15 ft (4.572 m) apart instead of D-16's original 30 m — each cluster's internal formation preserved, only translated as a rigid group | User: "I don't have a good landmark other than just knowing myself" — repositioning animals by coordinate previously meant reading `Transform3D` off the `Player` node in the editor's Remote debugger tab (extra clicks, only available while running from the editor); a small always-visible label removes that friction going forward ("that will be very helpful as we develop"). The 15 ft gap is a deliberate reduction from D-16's 30 m — the user wants the two flocks closer together now, still just far enough to read as separate | Easy — the HUD addition is purely additive (new `Label`/`Panel` nodes in `minimap.tscn`, one new line in `minimap.gd`'s `_process`); the recenter is a translation-only transform change per bird, each cluster's shape unchanged | fsamuels |
| D-28 | 2026-09-16 | (1) Fix the horse model facing 180° backwards while walking — `horse.tscn`'s `Model` node now applies a 180°-Y rotation alongside its existing `0.2317` scale. (2) Switch `horse.gd` from radius-around-home wandering to roaming a shared exported rectangle (`roam_min_x/max_x/min_z/max_z`, defaulting to `(163.2, 30.6)`–`(249.1, 39.8)`, a pasture strip near Turn Out Shed 1 identified using the new minimap coordinate readout, D-27). (3) Add a second `Horse` instance (`Horse2` in `main.tscn`) roaming the same rectangle | User: "they look like they are moonwalking!" — `horse.gd`'s `look_at()` correctly orients the root node's `-Z` toward the direction of travel every frame, but the Quaternius model's own local forward was `+Z`, not `-Z` (the opposite mismatch from D-26's TR6, which was already correct) — so the horse's body faced *away* from where it was walking while the `Walk` animation's leg cycle kept playing normally relative to the body, producing exactly the moonwalk illusion (body facing backward, legs still "stepping forward" relative to itself). Never caught earlier because a single horse circling one fixed home point rarely gets watched walking toward the camera from enough angles to notice; adding a second horse and a long rectangular roam path made it obvious immediately. The rectangle replaces `WANDER_RADIUS`'s circle-around-spawn model because the user gave two opposite-corner coordinates directly, not a center-and-radius — `_pick_new_target()` now samples X/Z independently and uniformly within the rectangle instead of polar-sampling a circle | Easy — the rotation fix is a one-line transform change affecting both horse instances identically (no risk of only fixing one); the rectangle-roam refactor removed `home_position`/`WANDER_RADIUS` entirely in favor of exported bounds, a small script change with no effect on the other animals' separate `wander.gd` | fsamuels |
| D-29 | 2026-09-16 | Re-derive the position, footprint size, and Y-axis rotation of the 5 buildings covered by the D-19 front-section orthomosaic (Shop, Sally's House, Turn Out Shed 1, Turn Out Shed 2, Front Barn) directly from that orthomosaic's own pixel data, rather than continuing to trust the D-5 Overture Maps ML footprints — moving the buildings to match the drone imagery, not the other way around, since the orthomosaic is real georeferenced UTM data (D-19, verified to ~1cm) while the ML footprints are the acknowledged-approximate side (D-5, 0.7–2.2m residual). For each building: cropped the orthomosaic around its ML-approximated position, manually identified the 4 roof corners in pixel space against an overlaid coordinate grid, converted those pixel corners to scene-local meters through the same pixel→world transform baked into the `FrontSectionOrthomosaic` node (D-19/D-21's row-major `Transform3D` convention), then fit a center, width, depth, and rotation angle from the 4 corners. All 5 buildings' `Transform3D`s were regenerated (previously axis-aligned diagonal scale only), and their dependent roof/gable child transforms (D-21–D-23) were re-derived from the same corner data using the general rotation-aware version of those decisions' geometry formulas, since a rotated building needs rotated roof/gable pieces too | User: "I would like to better align the buildings to the drone photos. They're off enough that it bothers me, especially at the barn" — Front Barn turned out to need the largest correction: real footprint is ~16.3m × 20.1m (ML guess: 14.8m × 23.1m) centered about 4.7m from its old position and rotated ~4.5° off scene axes, which the old axis-aligned-only model couldn't represent at all. Turn Out Shed 1/2 also carry small real rotations (Turn Out Shed 1: ~-4.4°; Turn Out Shed 2: ~-0.3°, notably different from Turn Out Shed 1 despite both being small ag outbuildings — re-verified by eye against the imagery rather than assumed uniform). Shop and Sally's House turned out close to axis-aligned in the scene's own frame (small corrections only, ~2-4m position, modest size change) — plausibly because they're built compass/UTM-aligned, which after the D-19 ~1.147° UTM-vs-scene-grid rotation shows up as walking away from a scene-axis rotation of ~0°, while the barn/sheds are angled to a different real-world reference (a fence line, not compass north). Verified visually: overlaid each new rotated footprint back onto the orthomosaic (independent of the Godot scene) and confirmed the box now traces the real roof outline for all 5 buildings, closest for Shop/Sally's House/Front Barn, good-not-perfect for the two Turn Out Sheds given hand-picked pixel corners' inherent precision limit (sub-meter, not millimeter). House and Back Barn were left untouched — they sit outside or at the edge of the D-19 flight coverage and still need the second drone flight (D-19's noted future work) before their footprints can be corrected the same way | Moderate — 5 building transforms plus their 10 roof and 10 gable child transforms all changed together (each dependent on the others via the shared rotation-aware geometry formulas); reversible by reverting this commit, but not a small diff. The corner-picking itself is manual/visual, not survey-grade, so residual error is expected to be well under a meter rather than exactly zero — a further pass with a proper corner-marking tool would only refine, not fundamentally redo, this work | fsamuels |
